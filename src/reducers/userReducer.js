const userReducer = (
    state = { username: '', email: '', jwt: '' },
    action
) => {

    switch (action.type) {
        default:
            break
    }

    return state
}

export default userReducer
